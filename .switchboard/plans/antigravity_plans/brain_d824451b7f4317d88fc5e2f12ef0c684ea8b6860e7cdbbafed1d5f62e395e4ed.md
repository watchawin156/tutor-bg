# Implementation Plan - Validation & PDF Reports

This plan outlines the changes needed to enforce payment limits during enrollment and implement the financial and schedule reports.

## Proposed Changes

### [Component] [App.tsx](file:///f:/webdev/รวมโปรเจคของโรงเรียน/TutorM/src/App.tsx)

#### [MODIFY] handleFormSubmit
- Add validation to ensure `payAmount` does not exceed the course price for the `enrollStudent` modal.

#### [MODIFY] UI & Reports
- Add `printMode` rendering logic to display the "Print Preview" screen when a report is selected.
- Integrate the original "New Window PDF" logic using a Blob/URL strategy.
- Add missing icons: `ArrowLeft`, `Printer`.

### [Component] [PdfService.ts](file:///f:/webdev/รวมโปรเจคของโรงเรียน/TutorM/src/utils/PdfService.ts) [NEW]
- Create a service to handle PDF generation using `pdf-lib` and `@pdf-lib/fontkit`.
- Implement Thai font (Sarabun) loading from a Google Fonts URL.
- Implement methods for `generateFinanceReport` and `generateTimetableReport`.
- Handle Opening the PDF in a new tab via Blob and `URL.createObjectURL`.

## Verification Plan

### Automated Tests
- Run `vite build` to ensure no syntax/type errors.

### Manual Verification
1. **Enrollment Validation**:
   - Open "Enroll Student" modal.
   - Enter a `payAmount` greater than the course price.
   - Verify that an alert appears and submission is blocked.
2. **PDF Reports**:
   - Navigate to the "Financial Summary" report from the sidebar.
   - Verify the preview screen appears.
   - Click "Open PDF in New Window" and verify a new tab opens with the correctly formatted report and a print dialog.
   - Repeat for "Timetable" report.
